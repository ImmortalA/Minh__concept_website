/**
 * Single source for mode names shown in the UI (toggle, hero watermarks, aria).
 * Change here only — keep in sync across the two views.
 */
export const VIEW_LABELS = {
  professional: "Professional",
  creative: "Creative",
} as const;
